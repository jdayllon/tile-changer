apiVersion: apps/v1
kind: Deployment
metadata:
  name: tile-changer-ign-deployment
  labels:
    app: tile-changer
    tier: backend
    target: ign
  namespace: tile-changer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tile-changer-ign
      tier: backend
      target: ign
  template:
    metadata:
      labels:
        app: tile-changer-ign
        tier: backend
        target: ign
    spec:
      containers:
      - name: tile-changer
        image: jdayllon/tile-changer:1.0.0
        ports:
          - containerPort: 80
        env:
         - name: TARGET_HOST
           valueFrom:
            configMapKeyRef:
              name: cfm-tile-changer
              key: TARGET_HOST_IGN